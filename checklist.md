# 스크롤 고정 위치 변경 체크리스트

- [x] 클로드의 로컬 커밋을 원격 최신 데이터 커밋 위에 재배치한다.
- [x] 랭킹 화면에서 필터 아래부터 순위 목록만 세로 스크롤되게 한다.
- [x] 선수 기록 화면에서 열 제목 아래부터 선수 행만 세로 스크롤되게 한다.
- [x] 내부 목록 스크롤과 당겨서 새로고침의 충돌을 막는다.
- [x] 웹 빌드로 검증한다.
- [x] 의미 단위로 커밋한다.
- [ ] 사용자 확인 후 원격 저장소에 반영한다.

## 100명 단위 추가 보기

- [x] 선수 기록 상세와 랭킹의 초기 표시 수를 100명으로 제한한다.
- [x] 하단 버튼으로 100명씩 추가 표시한다.
- [x] 정렬 또는 필터 변경 시 상위 100명부터 다시 표시한다.
- [x] 웹 빌드로 검증한다.

## 랭킹 폭 축소 + 상대전적 겸업 검색 (2026-07-12)

- [x] 모바일 랭킹 목록을 시즌 리더 카드 폭에 맞춤 — .rank-list 좌우 18px 인셋 + rank-row 값 컬럼 90px→auto(콘텐츠 폭). 값이 화면 끝까지 늘어지던 것 해소(값 우측 x341, 시즌 리더 x331 정렬). 데스크탑 무변경.
- [x] PlayerIndexEntry 에 hasBatting/hasPitching 추가(web+scraper types) + accumulate.ts 인덱스 생성에 반영.
- [x] index.json 을 player 파일 기준으로 패치(전체 재집계 없이 플래그만 주입, 2458명·겸업 511명).
- [x] matchup.ts inRole 을 등록 포지션이 아닌 실제 기록 유무 기준으로 변경(옛 인덱스는 포지션 폴백).
- [x] scraper 테스트·web tsc·프로덕션 빌드 통과, 모바일 dev 서버 실측(엄준상 투수 검색→배재고 상대 타자 11명 표시, 랭킹 행이 시즌 리더 폭과 정렬·메타 잘림 없음).
- [ ] 사용자 확인 후 커밋·원격 반영.

## 선수 기록 상세 필터·기본 정렬 (2026-07-25)

- [x] 필터 값을 URL에 저장해 다른 페이지에서 뒤로 돌아와도 복원한다.
- [x] 타자 기본·세부 탭을 타석 내림차순으로 시작한다.
- [x] 투수 기본·세부 탭을 이닝 내림차순으로 시작한다.
- [x] 탭 전환 시 해당 탭의 기본 정렬을 다시 적용한다.
- [x] 웹 빌드를 통과하고 로컬 검증 서버를 실행한다.

## u81-baseball 동기화 제거 (2026-08-08)

- [x] 동기화 전용 `sunset` 출력과 종료 게이트의 사용처를 확인한다.
- [x] `scrape.yml`의 일정 동기화 잡과 전용 게이트를 제거한다.
- [x] `teams.yml`의 선수현황 동기화 잡과 전용 게이트를 제거한다.
- [x] `scrape-full.yml`의 전체 동기화 잡과 전용 게이트를 제거한다.
- [x] YAML 구문과 잔여 동기화 참조를 검증한다.
- [x] 의미 단위로 커밋한다.

## 설치형 PWA 세로 화면 고정 (2026-08-08)

- [x] 현재 manifest 방향 설정과 PWA 초기화 경로를 확인한다.
- [x] manifest 기본 방향을 `portrait-primary`로 변경한다.
- [x] 설치형 PWA에서만 런타임 방향 잠금을 요청한다.
- [x] 일반 모바일 브라우저 동작이 바뀌지 않았는지 확인한다.
- [x] 웹 프로덕션 빌드로 검증한다.
- [x] 클로드의 푸시 전 검토용 체크리스트를 작성한다.
- [x] 클로드의 검토를 완료한다.
- [x] 의미 단위로 커밋한다.
