# 작업 맥락 기록

## 2026-07-12 스크롤 고정 경계 변경

- 첨부 이미지의 빨간 선을 실제 스크롤 경계로 사용한다.
- 랭킹은 필터 및 옵션 아래의 순위 목록만 스크롤한다.
- 선수 기록은 필터 및 옵션을 고정하고 표의 열 제목 아래 데이터 행만 스크롤한다.
- 동적 필터 높이를 계산하지 않고 모바일 페이지를 세로 flex 레이아웃으로 나눠 남은 공간만 스크롤한다.
- 내부 목록이 위로 스크롤된 상태에서는 당겨서 새로고침이 시작되지 않게 한다.

## 2026-07-12 필터 폭 통일

- 모바일의 랭킹 항목과 전체 시즌 선택은 지역 선택 필드와 같은 3분의 1 폭으로 통일한다.
- 데스크톱의 전체 시즌 선택은 지역 선택과 같은 200px 폭으로 통일한다.

## 2026-07-12 100명 단위 추가 보기

- 선수 기록 상세와 랭킹은 처음 100명만 렌더링한다.
- 하단 버튼을 누를 때마다 100명씩 추가하며, 마지막 묶음이 표시되면 버튼을 숨긴다.
- 정렬·필터·랭킹 항목이 달라지면 상위 100명부터 다시 시작한다.

## 2026-07-12 모바일 랭킹 폭 + 상대전적 겸업 검색 (커밋/푸시 전, 사용자 확인 대기)

- **모바일 랭킹 폭**: 랭킹 순위 행이 화면 끝까지 늘어져 값이 우측 가장자리에 붙던 것을 시즌 리더 카드 폭에 맞춤. `web/src/mobile/mobile.css` 의 `.m-page.leaders-page .rank-list` 에 좌우 18px 인셋(box-sizing border-box) + `.rank-row` 값 컬럼을 `90px→auto`(콘텐츠 폭). 결과 값 우측 x341(시즌 리더 x331과 정렬), 메타 잘림·가로 스크롤 없음. **데스크탑은 무변경**(처음 데스크탑에 넣었다가 되돌림 — app.css 는 EOL만 바뀌어 git checkout 으로 원복, 실질 변경 없음).
- **상대전적 겸업 검색**: 타자로 등록됐어도 투구 기록이 있으면 투수 검색에, 반대도 되도록 역할 판정을 등록 포지션이 아닌 실제 기록 유무 기준으로 변경. 데스크탑/모바일 공용.
  - `PlayerIndexEntry` 에 `hasBatting`/`hasPitching` 추가 — `web/src/shared/types.ts` + `scraper/src/types.ts`.
  - `scraper/src/accumulate.ts` 인덱스 생성부(약 501행)에서 `hasBatting: !!p.batting`, `hasPitching: !!p.pitching`.
  - `data/2026/players/index.json` 은 전체 재집계(타임스탬프 churn) 대신 각 `players/{id}.json` 의 batting/pitching 유무로 플래그만 주입해 패치(2458명·겸업 511명). CI 재집계 시 accumulate.ts 가 동일 값을 내므로 일관.
  - `web/src/shared/matchup.ts` 의 `inRole` 을 `role==='pitcher' ? p.hasPitching : p.hasBatting`(플래그 없는 옛 인덱스는 포지션 폴백)로 변경.
- **검증**: scraper 테스트 3종 통과, web `tsc -b` 0, Vite 프로덕션 빌드 통과. 모바일 dev 서버 실측 — 엄준상(등록 내야수, 투구 5경기·12.2이닝) 투수 검색→상대 학교 3개(광주제일고·배재고·야탑고)→배재고 상대 타자 11명 개별 기록 표시. 랭킹 행이 시즌 리더 폭과 정렬됨.
- **남은 일**: 사용자 확인 후 의미 단위 커밋(① 데이터 index.json ② scraper 타입/집계 ③ web 상대전적 로직/타입 ④ 모바일 CSS 정도로 분리 가능). 푸시는 사용자 지시 전 금지.

## 2026-07-12 새로고침 모션 순서 교체

- 3번 스코어보드 플립을 1번으로 올려 앱 최초 실행과 선택값 누락 시 기본 모션으로 사용한다.
- 1번 홈런 아크는 3번으로 내려 새로고침 랜덤 후보의 세 번째 순서가 된다.
- 2번 패스트 피치는 유지한다.

## 2026-07-12 수동 새로고침 모션 순환

- 앱 최초 실행은 1번 스코어보드 플립만 재생하며 순환 인덱스를 바꾸지 않는다.
- 수동 새로고침은 2번 패스트 피치 → 3번 홈런 아크 → 1번 스코어보드 플립 순서로 반복한다.

## 2026-07-25 선수 기록 상세 필터·기본 정렬

- 필터는 컴포넌트 내부 상태에만 두지 않고 `/records` URL 쿼리에 저장한다. 브라우저 뒤로가기로 페이지가 다시 마운트될 때 동일 값을 복원할 수 있다.
- 타자 두 탭은 타석(`pa`), 투수 두 탭은 이닝(`ip`)을 기본 1순위 내림차순으로 사용한다.
- 세부 탭에도 기본 정렬 기준과 정렬 표시가 보이도록 타석·이닝 열을 추가한다.

## 2026-08-08 u81-baseball 동기화 제거

- `namgungjinwonng/u81-baseball` 서비스 종료 후 더 이상 데이터를 동기화하지 않는다.
- `scrape.yml`, `teams.yml`, `scrape-full.yml`의 `sync-merge` 잡 전체를 제거한다.
- `sunset` 출력과 종료 게이트는 `sync-merge` 조건에만 사용되므로 함께 제거한다.
- 현재 저장소의 데이터 수집, `data/` 커밋 및 `origin/main` 푸시 동작은 유지한다.
- `PROJECT_REFERENCE.md`의 기존 기록은 과거 변경 이력이므로 수정하지 않는다.
- 세 워크플로 모두 YAML 파싱에 성공했으며 Actions 워크플로 내부에 동기화 전용 참조가 남지 않았다.

## 2026-08-08 설치형 PWA 세로 화면 고정

- 일반 모바일 브라우저에는 회전 안내나 방향 잠금을 적용하지 않는다.
- 설치형 PWA는 manifest의 기본 방향을 `portrait-primary`로 선언한다.
- 설치형 실행이 감지될 때 지원되는 환경에서 `screen.orientation.lock("portrait-primary")`을 요청한다.
- 방향 잠금 API가 없거나 거부되는 환경에서는 오류를 노출하지 않고 manifest 동작만 사용한다.
- 프로젝트의 TypeScript DOM 정의에는 `ScreenOrientation.lock()`이 없어 호출 지점에서만 선택적 API 타입을 선언한다.
- `npm.cmd run build`로 TypeScript와 Vite 프로덕션 빌드를 통과했으며 산출물 manifest에도 `portrait-primary`가 반영됐다.
- 푸시 전 독립 검토용 `claude-review-checklist.md`는 로컬 전용이라 커밋하지 않는다.
- 클로드 검토 결과 결함은 없었다. `isStandalone`을 `initPwa` 위로 옮긴 것은 필수 변경이다. `main.tsx`가 모듈 평가 시점에 `initPwa()`를 호출하므로 원래 위치의 `const`로 두면 TDZ `ReferenceError`가 난다.
- 이미 설치된 Android PWA는 재설치해야 세로 고정이 적용됐다. 실기기 확인 결과 새 번들이 배포된 뒤에도 회전이 계속됐고, 앱을 삭제 후 재설치하자 고정됐다. 실제로 동작한 경로는 manifest의 `portrait-primary`가 재설치 시 안드로이드 액티비티 방향에 반영되는 것이고, 런타임 `lock()`은 그 기기에서 효과가 없었다.
- 다만 런타임 `lock()` 코드는 제거하지 않고 남겨둔다. 다른 기기나 크롬 버전에서 동작할 여지가 있고 실패해도 무해하다.
- 새로 설치하는 사용자는 처음부터 세로로 고정되므로 재설치 문제는 기존 설치자에 한정된다.
- iOS 홈 화면 앱은 manifest `orientation`을 무시하고 사파리에 `lock()`도 없어 세로 고정이 적용되지 않는다. 안드로이드 설치형 전용 기능이다.

## 2026-08-08 학교별 랭킹 탭

- `codex-task-school-ranking.md`의 확인된 데이터 사실을 재조사하지 않고 구현 기준으로 사용한다.
- `scraper/`, 데이터 JSON, 라우트와 기존 선수 랭킹 계산은 수정하지 않는다.
- 학교 탭은 URL의 기존 필터 쿼리를 보존하지만 학교·학년 값은 팀 집계에 적용하지 않고 지역·대회만 적용한다.
- `StatTable`의 정렬 로직은 재사용하고 셀 렌더 함수에 정렬 후 행 인덱스만 전달해 표시 순위를 계산한다.
- 승차 기준 팀은 현재 지역 필터 안에서 승률 내림차순, 승수 내림차순, 패수 오름차순의 첫 팀으로 정한다.
- 작업 완료 후에도 커밋·푸시하지 않고 클로드 검토용 체크리스트를 별도로 남긴다.
- `claude-review-school-ranking.md`에 계산식, UI, 금지 범위와 실행 명령을 분리해 기록한다.
- 합성 단위 테스트와 웹 프로덕션 빌드가 통과했다.
- 실제 2026 시즌 데이터 적용 결과 103개 팀이 생성됐고 숫자 필드에 `NaN`이나 무한값이 없음을 확인했다.

## 2026-08-08 학교 행에서 선수 기록으로 이동

- 클로드가 먼저 반영한 팀명 정규화·표 단위·정렬·CSS 변경은 되돌리지 않고 후속 작업만 추가한다.
- 학교별 랭킹 행의 정규화된 팀명과 선수 기록의 원본 팀명을 `teamDisplayName()`으로 양쪽 정규화해 비교한다.
- 학교 드롭다운과 학년 캐스케이드도 같은 정규화 비교를 사용해 URL 선택값과 표시값을 일치시킨다.
- 학교 행 클릭 시 현재 대회·지역 필터와 클릭한 학교를 `/records`에 전달하고 학년은 명시적으로 빈 값으로 초기화한다.
- 작업 완료 후에도 커밋·푸시하지 않고 클로드가 실제 화면과 diff를 검토한다.
- `filters.test.ts`는 실제 `filters.tsx`를 Vite SSR 모듈 로더로 불러와 Vite 전용 `import.meta.env` 의존성을 유지한 채 `applyFilter`를 검증한다.
- 기존 학교 집계 테스트, 새 필터 정규화 테스트와 웹 프로덕션 빌드가 모두 통과했다.
- 위 기록에 등장하는 `codex-task-school-ranking.md`, `claude-review-school-ranking.md`, `claude-review-checklist.md`는 작업·검토용 임시 문서였다. 로컬 전용으로만 쓰고 작업 종료 시 삭제했으므로 저장소에는 없다.
- 데이터 갱신으로 신규 학교가 들어오는 경우를 9가지 시나리오로 검증했다. 경기만 있고 선수현황에 없는 학교는 지역이 빈 값으로 표시되고 지역 필터 적용 시 제외되며, 선수현황이 갱신되면 자연히 채워진다. 어느 경우에도 `NaN`·무한값·예외가 발생하지 않는다.

## 2026-08-08 랭킹 항목 이전·다음 선택기

- 기존 타자·투수 항목 드롭다운을 B안인 이전·현재·다음 선택기로 교체한다.
- 항목 순서는 기존 `CATEGORIES` 배열 순서를 그대로 사용하며 마지막과 처음은 순환한다.
- 전체 항목 패널은 페이지 최초 진입 시 접힌 상태다.
- 패널 안에서 항목을 선택해도 열린 상태를 유지하고, 사용자가 `전체 항목 닫기`를 눌렀을 때만 접는다.
- 학교 랭킹 탭에는 선수 항목 선택기를 표시하지 않는다.
- 현재 워킹 트리의 학교 랭킹 및 클로드 수정분은 보존하고 이번 선택기 관련 줄만 추가·교체한다.
- 이번 후속 작업도 클로드 검토 전에는 커밋하거나 푸시하지 않는다.
- `categoryPanelOpen`은 항목 링크 선택 시 변경하지 않는다. 동일한 `/leaders/:id` 라우트 안에서 항목만 바뀌므로 열린 패널이 유지된다.
- 전체 항목 토글 버튼만 이 상태를 변경하며 최초값은 `false`다.
- 학교 집계 테스트와 학교 필터 테스트가 통과했고 웹 프로덕션 빌드 및 `git diff --check`도 통과했다.
- 사용자 실화면 확인에 따라 이전·현재·다음 영역을 세 개의 분리 박스가 아닌 하나의 얇은 테두리 안에 한 줄로 표시하도록 시안과 맞췄다.
- 접힌 상태의 전체 항목 버튼은 높이와 세로 여백을 기존의 약 절반으로 줄이고, 펼친 상태의 닫기 버튼은 기존 조작 높이를 유지한다.

## 2026-08-08 폴더블 펼친 화면 레이아웃 개선

- 갤럭시 Z 폴드8의 펼친 가로 화면은 CSS viewport가 기존 모바일 상한 1024px을 넘어서 데스크톱 컴포넌트 트리로 분기됐다.
- 데스크톱 홈의 1440px 컨테이너와 자동 확장 리더 그리드가 좁은 폴더블 화면 전체를 사용하면서 카드가 좌우로 과도하게 펼쳐졌다.
- 터치가 주 입력인 최대 1366px 화면은 폴더블·태블릿으로 보고 모바일 트리를 사용한다. 일반 마우스 기반 노트북과 데스크톱은 기존 1024px 경계를 유지한다.
- 모바일 트리는 본문과 상단바 내부 폭을 이미 720px로 제한하므로 폴드8 가로 화면에서도 콘텐츠가 가운데 모인다.
- `mobile.css`의 공용 필터 행 규칙은 `.m-shell` 아래로 제한해 데스크톱 필터 폭을 덮어쓰지 않게 한다.
- 이번 후속 작업도 클로드 검토 전에는 커밋하거나 푸시하지 않는다.
- 화면 분기 순수 함수 테스트에서 외부 모바일 390px, 기존 경계 1024px, 폴더블 터치 1224px, 같은 폭의 마우스 화면, 1367px 초과 터치 화면과 강제 데스크톱 쿼리를 검증했다.
- 화면 분기 테스트, 웹 프로덕션 빌드와 `git diff --check`가 모두 통과했다.
- 기존 클로드 검토 문서는 앞선 작업 종료 시 삭제된 상태이므로 다시 만들지 않고 `checklist.md`와 이 기록에 검증 결과를 남긴다.
